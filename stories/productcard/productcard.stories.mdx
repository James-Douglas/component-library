import { Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { ProductCard } from '@comparethemarketau/manor-product-card';
import { Typography } from '@comparethemarketau/manor-typography';
import { ManorProvider } from '@comparethemarketau/manor-provider';
import originLogo from '../../images/ORIGIN.png';
import response from './response.dummy';


<Meta title="Components/ProductCard" component={ProductCard} />

# Product Card

A card representing a single result from a product quote.

**Logo**, **Name**, and **Price** are common to *all* products and as such can be provided directly as props. Vertical specific product information must be passed through as children.

Resize the window to see different responsive layouts.

<Preview>
  <Story name="product card">\
    <ProductCard
      product={{
        name: "Test Product Discount Saver Max",
        logo: originLogo,
        cost: {
          price: 632.28033,
          period: 'ANNUALLY',
          intervals: ['ANNUALLY', 'MONTHLY', 'QUARTERLY'],
          intervalText: 'Est. cost per'
        },
        priceDisclaimer: 'Incl. GST & conditional discounts',
      }}
      additionalContent={<Typography variant="body2" style={{ fontSize: '1.5rem' }}>Some more product info, or whatever.</Typography>}
      productDocuments={<Typography variant="body2" style={{ marginTop: '1rem', fontSize: '1.2rem' }}>Plan Information Documents</Typography>}
    >
      <Typography variant="body2" style={{ fontSize: '1.5rem', marginRight: '1rem' }}>Vertical Specific Info #1</Typography>
      <Typography variant="body2" style={{ fontSize: '1.5rem', marginRight: '1rem' }}>Vertical Specific Info #2</Typography>
      <Typography variant="body2" style={{ fontSize: '1.5rem' }}>Vertical Specific Info #3</Typography>
    </ProductCard>
  </Story>
</Preview>

## Props
<Props of={ProductCard} />
