import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, text, object, select } from '@storybook/addon-knobs';
import LinkTo from '@storybook/addon-links/react';
import { Logo, CTMLogo } from '@comparethemarketau/manor-logo';
import { ctmTheme } from '@comparethemarketau/manor-themes';
import CTMLogoStacked from '../../images/ctm-logo-stacked.svg';
import CTMLogoInline from '../../images/ctm-logo-inline.svg';

<Meta title="Components/Logo" component={Logo} />

# Logo

<Preview>
  <Story name="logo">
    <Logo
      size="large"
      link="https://www.comparethemarket.com.au"
      picture={{
        src: CTMLogoStacked,
        srcsets: [
          {
            srcset: CTMLogoStacked,
            media: `(max-width: ${ctmTheme.breakpoints.lg})`,
          },
          {
            srcset: CTMLogoInline,
          }
        ],
      }}
   />
  </Story>
</Preview>

### Props (Logo)

*picture* props can be found on the <LinkTo kind="components-picture--picture">Picture</LinkTo> component.

<Props of={Logo} />

## CTMLogo

For convenience, a `CTMLogo` component has been created which renders a `Logo` with the expected props based on the
design system.

### Props (CTMLogo)

<Props of={CTMLogo} />
