import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, text, boolean, number, select, object } from '@storybook/addon-knobs';
import { ExpressiveInput } from '@comparethemarketau/manor-input';
import PrefixSuffixView from './expressive-prefix-suffix';
import GreyBackgroundView from './expressive-on-grey-background';

<Meta title="Components/Input/ExpressiveInput" component={ExpressiveInput} />

# Expressive Input

This component has the base components functionality in terms of props to use, but it has design modifications that are **only** to be used in short/lite forms. It also has a larger variant at XL breakpoint and up

Can support multiple formats including numbers and masks



<Preview>
  <Story name="Expressive">
    <div style={{ padding: '2%' }}>
      <ExpressiveInput
        label="Label"
        id="Expressive-input-one"
        placeholder="Placeholder"
        required={false}
        handleChange={() => {}}
      />
    </div>
  </Story>
</Preview>

## Props

<Props of={ExpressiveInput} />

<Story name="Expressive with props" decorators={[withKnobs]} parameters={{ docs: { disable: true } }}>
  {() => {
    const componentPropsId = 'Component Props';
    const label = text('label', 'Expressive Demo', componentPropsId);
    const validationMessage = text('validationMessage', '', componentPropsId);
    const maxLength = number('maxLength', 25, {}, componentPropsId);
    const value = text('value', '', componentPropsId);
    const prefillValue = text('prefillValue', '', componentPropsId);
    const placeholder = text('placeholder', 'Placeholder', componentPropsId);
    const required = boolean('required', false, componentPropsId);
    const disabled = boolean('disabled', false, componentPropsId);
    const readonly = boolean('readonly', false, componentPropsId);
    const prefix = text('prefix', '', componentPropsId);
    const suffix = text('suffix', '', componentPropsId);
    return (
      <div style={{ padding: '2%' }}>
        <ExpressiveInput
          id="Expressive-input"
          label={label}
          validationMessage={validationMessage}
          maxLength={maxLength}
          value={value}
          prefillValue={prefillValue}
          placeholder={placeholder}
          disabled={disabled}
          readonly={readonly}
          required={required}
          prefixContent={prefix}
          suffixContent={suffix}
          handleChange={() => {}}
        />
      </div>
    )
  }}
</Story>

<Story name="expressive input grey background"  parameters={{ docs: { disable: true } }}>
  <GreyBackgroundView />
</Story>

<Story name="expressive input prefix and suffix"  parameters={{ docs: { disable: true } }}>
  <PrefixSuffixView />
</Story>
