import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { useState } from 'react';
import { DateRangePicker } from '@comparethemarketau/manor-date-picker';
import { FluidContainer, Row, Column } from '@comparethemarketau/manor-grid';
import moment from 'moment';

<Meta title="Components/DatePicker/DateRangePicker" component={DateRangePicker} />

# Date Picker Range
<Preview>
  <Story name="date range picker" height="500px">
    {() => {
      const [startDate, setStartDate] = useState(moment());
      const [endDate, setEndDate] = useState(null);
      return (
        <div style={{ padding: '2%' }}>
           <FluidContainer>
            <Row>
              <Column>
                <DateRangePicker
                  startDateId="start-date"
                  startDateTooltip={{ title: 'Start Date' }}
                  startDateAriaLabel="Start Date"
                  startDateValue={startDate}
                  endDateId="end-date"
                  endDateTooltip={{ title: 'End Date' }}
                  endDateAriaLabel="End Date"
                  endDateValue={endDate}
                  numberOfMonths={2}
                  endDateValidationMessage="Incorrect end date"
                  startDateValidationMessage="Incorrect start date"
                  isDayBlocked={(day) => day.weekday() === 4}
                  handleChange={(dates) => console.log('handleChange', dates)}
                />
                </Column>
              </Row>
            </FluidContainer>
        </div>
      );
    }}
  </Story>
</Preview>

### Props
<Props of={DateRangePicker} />
