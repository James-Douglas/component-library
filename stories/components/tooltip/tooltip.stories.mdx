import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, text, select } from '@storybook/addon-knobs';
import { Tooltip } from '@comparethemarketau/manor-tooltip';
import { Typography } from '@comparethemarketau/manor-typography';
import getTooltipKnobs from '../../tooltipKnobs';

<Meta title="Components/Tooltip" component={Tooltip} />

# Tooltip

The Tooltip component is used to display additional information on hover over the tooltip icon.

Form components will typically expose a tooltip prop, internally form components handle
tooltip positioning.

<Preview>
  <Story name="tooltip" height="150px">
    <div style={{ padding: '2%'}}>
      <div style={{ display: 'flex', height: '100%', justifyContent: 'center', alignItems: 'center', flexDirection: 'column' }}>
        <Typography variant="h4">Standard Tooltip</Typography>
        <Tooltip trackingLabel="standard tooltip" title="tooltip title" body="tooltip body" />
        <Typography variant="h4">Tooltip with text (children)</Typography>
        <Tooltip trackingLabel="tooltip with text" title="tooltip title" body="tooltip body">
          <Typography variant="body1" style={{ color: '#0b60b7' }}>what's this?</Typography>
        </Tooltip>
        <Typography variant="h4">Advanced Tooltip body</Typography>
        <Tooltip trackingLabel="advanced tooltip body" body={<><strong>My subtitle</strong><br/>My description</>} />
        <Typography variant="h4">Tooltip with larger icon</Typography>
        <Tooltip iconSize={'5x'} trackingLabel="tooltip with larger icon" body={<><strong>Larger Icon</strong><br/>The icon to open me is larger, yes?</>} />
      </div>
    </div>
  </Story>
</Preview>

### Props
<Props of={Tooltip} />

<Story name="tooltip with props" height="150px" decorators={[withKnobs]} parameters={{ docs: { disable: true } }}>
  {() => {
    const { title, body, placement, screenReaderLabel, variant }= getTooltipKnobs();
    return (
      <div style={{ padding: '2%'}}>
        <div style={{ display: 'flex', height: '200px', justifyContent: 'center', alignItems: 'center', background: variant === 'light' ? 'grey' : 'inherit' }}>
          <Tooltip trackingLabel="tooltip with props" title={title} body={body} placement={placement} variant={variant} screenReaderLabel={screenReaderLabel} />
        </div>
      </div>
    );
  }}
</Story>
