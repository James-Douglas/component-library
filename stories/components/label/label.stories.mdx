import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import {
  withKnobs,
  text,
  boolean,
  number,
  select,
  object,
} from '@storybook/addon-knobs';

import { Label } from '@comparethemarketau/manor-label';

<Meta title="Components/Label" />

# Label

Labels are used to provide a label for form elements

<Preview>
  <Story name="label">
    <DemoContainer>
      <Label text="Label Component" />
    </DemoContainer>
  </Story>
</Preview>

<Story
  name="label with props"
  decorators={[withKnobs]}
  parameters={{ docs: { disable: true } }}
>
  {() => {
    const variant = select('variant', ['default', 'compact'], 'outlined');
    return (
      <DemoContainer>
        <Label variant={variant} text="Example Field Label" tooltip={{ trackingLabel: 'label example tooltip', body: 'Example Tooltip' }} />
      </DemoContainer>
    );
  }}
</Story>

#### Props

<Props of={Label} />
