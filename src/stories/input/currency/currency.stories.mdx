import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, text, boolean, number, select, object } from '@storybook/addon-knobs';

import CurrencyInput from '../../../components/Input/Currency/CurrencyInput.component';
import getTooltipKnobs from '../../tooltipKnobs';

<Meta title="Components/Input/Currency" component={CurrencyInput} />

# Currency Input

<Preview>
  <Story name="currency">
    <CurrencyInput
      label="Currency input"
      id="currency-input-one"
      tooltip={{title: 'currency demo'}}
      placeholder="12345"
      currencySymbol="$"
      bordered
      required={false}
      disabled={false}
      invalid={false}
      handleChange={() => {}}
    />
  </Story>
</Preview>

## Props

<Props of={CurrencyInput} />

<Story name="currency with props" decorators={[withKnobs]} parameters={{ docs: { disable: true } }}>
  {() => {
    const componentPropsId = 'Component Props';
    const tooltipGroupId = 'Tooltip Props';
    const label = text('label', 'Currency demo', componentPropsId);
    const validationMessage = text('validationMessage', '', componentPropsId);
    const maxLength = number('maxLength', 25, {}, componentPropsId);
    const value = text('value', '', componentPropsId);
    const prefillValue = text('prefillValue', '1', componentPropsId);
    const placeholder = text('placeholder', '12345', componentPropsId);
    const required = boolean('required', false, componentPropsId);
    const disabled = boolean('disabled', false, componentPropsId);
    const bordered = boolean('bordered', true, componentPropsId);
    const currencySymbol = text('currencySymbol', '$', componentPropsId);
    const { title, body, placement, screenReaderLabel, variant } = getTooltipKnobs(tooltipGroupId);
    return (
      <CurrencyInput
        label={label}
        id="currency-input-one"
        tooltip={{
          title,
          body,
          placement,
          screenReaderLabel,
          variant
        }}
        validationMessage={validationMessage}
        maxLength={maxLength}
        value={value}
        prefillValue={prefillValue}
        placeholder={placeholder}
        currencySymbol={currencySymbol}
        bordered={bordered}
        required={required}
        disabled={disabled}
        handleChange={() => {}}
      />
    )
  }}
</Story>
