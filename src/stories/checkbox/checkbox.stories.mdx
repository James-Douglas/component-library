import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import Checkbox from '../../components/Checkbox/Checkbox.component';
import CheckboxGroup from '../../components/Checkbox/CheckboxGroup.component';
import CheckboxGroupView from './checkboxGroup.view';
import FluidContainer from '../../components/Grid/Container/FluidContainer.component';
import Container from '../../components/Grid/Container/Container.component';
import Row from '../../components/Grid/Row/Row.component';
import Column from '../../components/Grid/Column/Column.component';
import { faTimes } from '@fortawesome/pro-regular-svg-icons/faTimes';

<Meta title="Components|Checkbox" component={Checkbox} />

# Checkbox

Checkboxes allow users to select one or more options by clicking on the checkbox or it's label. In Manor, checkboxes can
be used singularly via the `Checkbox` component, or can be grouped logically via the `CheckboxGroup` component.

<Preview>
  <Story name="checkbox">
    <FluidContainer>
      <Row>
        <Column>
          <Checkbox id="1" label="A checkbox" />
        </Column>
      </Row>
    </FluidContainer>
  </Story>
</Preview>

<Preview>
  <Story name="checkbox group">
    <FluidContainer>
      <Row>
        <Column>
          <CheckboxGroup
            label="Checkbox group example"
            tooltip={{ title: 'Checkbox group', body: 'This is a tooltip for the checkbox group' }}
            groupId="group-1"
            colSize="12"
            handleChange={() => {}}
          >
            <Checkbox id="A-1" label="One" />
            <Checkbox id="A-2" label="Two" />
            <Checkbox id="A-3" label="Three" />
          </CheckboxGroup>
        </Column>
      </Row>
    </FluidContainer>
  </Story>
</Preview>

<Story name="checkbox group demo" parameters={{ docs: { disable: true } }}>
  <CheckboxGroupView />
</Story>

## Behaviour

* Clicking on either the checkbox or it's label will toggle the checkbox.

### Accessibility
* Checkboxes can be focused via the `Tab` key.
* Checkboxes can be toggled via the `Space` key when focused.

### Checkbox Props

<Props of={Checkbox} />

### CheckboxGroup Props

<Props of={CheckboxGroup} />
