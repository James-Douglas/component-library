import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, text, object } from '@storybook/addon-knobs';
import placeholder from '../../images/placeholder.png';
import Picture from '../../components/Picture/Picture.component';

<Meta title="Components|Picture" component={Picture} />

# Picture

Responsive picture component

<Preview>
  <Story name="picture">
     <Picture alt="placeholder" title="title"/>
  </Story>
</Preview>

# Props

<Props of={Picture} />

<Story name="picture with props" decorators={[withKnobs]} parameters={{ docs: { disable: true } }}>
  {() => {
    const src = text('src', '');
    const srcsets = object('srcsets', [
      {
        srcset: placeholder,
      }
    ]);
    const alt = text('alt', '');
    const title = text('title', '');
    return <Picture srcsets={srcsets} src={src} alt={alt} title={title} />
  }}
</Story>
