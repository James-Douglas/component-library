import { useState } from 'react';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, boolean} from '@storybook/addon-knobs';
import Notification from '../../components/Notification/Notification.component';
import Container from '../../components/Grid/Container/Container.component';
import Row from '../../components/Grid/Row/Row.component';
import Column from '../../components/Grid/Column/Column.component';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faLongArrowLeft } from '@fortawesome/pro-regular-svg-icons/faLongArrowLeft';
import { faInfoCircle } from '@fortawesome/pro-regular-svg-icons/faInfoCircle';

<Meta title="Components|Notification" component={Notification} />

# Notification

<Preview>
  <Story name="general notification/toast">
    {() => {
      const [ isVisibleToast, setIsVisibleToast ] = useState(true);
      return (
        <>
          <Container>
            <Row>
              <Column offset="1" cols="10">
                <div style={{margin: '50px 0', width: '100%'}}>
                  <Notification type="primary">
                    <h5 style={{margin: '0 0 5px', color: 'black'}}>Notification title goes here</h5>
                    The insurance provider will capture the full description and replacement value of the item(s) you wish to specify later.
                  </Notification>
                </div>
              </Column>
            </Row>
            <Row>
              <Column offset="1" cols="10">
                <div style={{margin: '50px 0', width: '50%'}}>
                  {isVisibleToast &&
                  <Notification type="primary" closeButton handleClose={() => setIsVisibleToast(false)}>
                    <h5 style={{margin: '0 0 5px', color: 'black'}}>Toast title goes here</h5>
                    Provider will capture the full description.
                  </Notification>
                  }
                  {!isVisibleToast && (
                    <div style={{fontSize: '30px'}}>
                      <a href="#" title="" style={{fontSize: '14px'}} target="_self" onClick={() => setIsVisibleToast(true)}><span style={{marginRight: '5px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Return notification</a>
                      <br />
                      Notification hidden
                    </div>
                  )}
                </div>
              </Column>
            </Row>
          </Container>
       </>
      );
    }}
  </Story>
</Preview>

<Preview>
  <Story name="primary notification">
    {() => {
      const [ isVisible, setIsVisible ] = useState(true);
      const [ isVisibleToast, setIsVisibleToast ] = useState(true);
      return (
        <>
          <Container>
            <Row>
              <Column offset="1" cols="10">
                <div style={{margin: '50px 0', width: '100%'}}>
                  {isVisible &&
                    <Notification type="primary" icon closeButton handleClose={() => setIsVisible(false)}>
                      <h5 style={{margin: '0 0 5px', color: 'black'}}>Notification title goes here</h5>
                      The insurance provider will capture the full description and replacement value of the item(s) you wish to specify later.
                      <div style={{marginTop: '20px', display: 'flex', justifyContent: 'space-between'}}>
                        <a href="#" title=""><span style={{marginRight: '6px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Action goes here</a>
                        <a href="#" title=""><span style={{marginRight: '6px'}}><FontAwesomeIcon icon={faInfoCircle} size="1x" /></span>Action goes here</a>
                      </div>
                    </Notification>
                  }
                  {!isVisible && (
                    <div style={{fontSize: '30px'}}>
                      <a href="#" title="" style={{fontSize: '14px'}} target="_self" onClick={() => setIsVisible(true)}><span style={{marginRight: '5px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Return notification</a>
                      <br />
                      Notification hidden
                    </div>
                  )}
                </div>
              </Column>
            </Row>
            <Row>
              <Column offset="1" cols="10">
                <div style={{margin: '50px 0', width: '100%'}}>
                  <Notification type="primary" icon>
                    <h5 style={{margin: '0 0 5px', color: 'black'}}>Notification title goes here</h5>
                    The insurance provider will capture the full description and replacement value of the item(s) you wish to specify later.
                    <div style={{marginTop: '20px', display: 'flex', justifyContent: 'space-between'}}>
                      <a href="#" title=""><span style={{marginRight: '6px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>See products with an Age-Based discount</a>
                    </div>
                  </Notification>
                </div>
              </Column>
            </Row>
            <Row>
              <Column offset="1" cols="10">
                <div style={{margin: '50px 0', width: '50%'}}>
                  {isVisibleToast &&
                    <Notification type="primary" icon closeButton handleClose={() => setIsVisibleToast(false)}>
                      <h5 style={{margin: '0 0 5px', color: 'black'}}>Notification title goes here</h5>
                      Provider will capture the full description.
                      <div style={{marginTop: '20px', display: 'flex', justifyContent: 'space-between'}}>
                        <a href="#" title=""><span style={{marginRight: '6px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Action goes here</a>
                      </div>
                    </Notification>
                  }
                  {!isVisibleToast && (
                    <div style={{fontSize: '30px'}}>
                      <a href="#" title="" style={{fontSize: '14px'}} target="_self" onClick={() => setIsVisibleToast(true)}><span style={{marginRight: '5px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Return notification</a>
                      <br />
                      Notification hidden
                    </div>
                  )}
                </div>
              </Column>
            </Row>
          </Container>
        </>
      );
    }}
  </Story>
</Preview>

<Preview>
  <Story name="success notification">
   {() => {
    const [ isVisibleToast, setIsVisibleToast ] = useState(true);
    return (
      <>
        <Container>
          <Row>
            <Column offset="1" cols="10">
              <div style={{margin: '50px 0', width: '100%'}}>
                <Notification type="success" icon>
                  <h5 style={{margin: '0 0 5px', color: 'black'}}>Notification title goes here</h5>
                  The insurance provider will capture the full description and replacement value of the item(s) you wish to specify later.
                  <div style={{marginTop: '20px', display: 'flex', justifyContent: 'space-between'}}>
                    <a href="#" title=""><span style={{marginRight: '6px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Action goes here</a>
                    <a href="#" title=""><span style={{marginRight: '6px'}}><FontAwesomeIcon icon={faInfoCircle} size="1x" /></span>Action goes here</a>
                  </div>
                </Notification>
              </div>
            </Column>
          </Row>
          <Row>
            <Column offset="1" cols="10">
              <div style={{margin: '50px 0', width: '100%'}}>
                <Notification type="success" icon>
                  <h5 style={{margin: '0 0 5px', color: 'black'}}>Notification title goes here</h5>
                  The insurance provider will capture the full description and replacement value of the item(s) you wish to specify later.
                  <div style={{marginTop: '20px', display: 'flex', justifyContent: 'space-between'}}>
                    <a href="#" title=""><span style={{marginRight: '6px'}}><FontAwesomeIcon icon={faInfoCircle} size="1x" /></span>What is an Age-Based Discount?</a>
                  </div>
                </Notification>
              </div>
            </Column>
          </Row>
          <Row>
            <Column offset="1" cols="10">
              <div style={{margin: '50px 0', width: '50%'}}>
                {isVisibleToast &&
                  <Notification type="success" icon closeButton handleClose={() => setIsVisibleToast(false)}>
                    <h5 style={{margin: '0 0 5px', color: 'black'}}>Notification title goes here</h5>
                    Provider will capture the full description.
                  </Notification>
                }
                {!isVisibleToast && (
                  <div style={{fontSize: '30px'}}>
                    <a href="#" title="" style={{fontSize: '14px'}} target="_self" onClick={() => setIsVisibleToast(true)}><span style={{marginRight: '5px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Return notification</a>
                    <br />
                    Notification hidden
                  </div>
                )}
              </div>
            </Column>
          </Row>
        </Container>
      </>
     );
    }}
  </Story>
</Preview>

<Preview>
  <Story name="warning notification">
    {() => {
      const [ isVisibleToast, setIsVisibleToast ] = useState(true);
      return (
        <>
          <Container>
            <Row>
              <Column offset="1" cols="10">
                <div style={{margin: '50px 0', width: '100%'}}>
                  <Notification type="warning" icon>
                    <h5 style={{margin: '0 0 5px', color: 'black'}}>Notification title goes here</h5>
                    The insurance provider will capture the full description and replacement value of the item(s) you wish to specify later.
                    <div style={{marginTop: '20px', display: 'flex', justifyContent: 'space-between'}}>
                      <a href="#" title=""><span style={{marginRight: '6px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Action goes here</a>
                      <a href="#" title=""><span style={{marginRight: '6px'}}><FontAwesomeIcon icon={faInfoCircle} size="1x" /></span>Action goes here</a>
                    </div>
                  </Notification>
                </div>
              </Column>
            </Row>
            <Row>
              <Column offset="1" cols="10">
                <div style={{margin: '50px 0', width: '100%'}}>
                  <Notification type="warning" icon>
                    <h5 style={{margin: '0 0 5px', color: 'black'}}>Notification title goes here</h5>
                    The insurance provider will capture the full description and replacement value of the item(s) you wish to specify later.
                    <div style={{marginTop: '20px', display: 'flex', justifyContent: 'space-between'}}>
                      <a href="#" title=""><span style={{marginRight: '6px'}}><FontAwesomeIcon icon={faInfoCircle} size="1x" /></span>What is an Age-Based Discount?</a>
                    </div>
                  </Notification>
                </div>
              </Column>
            </Row>
            <Row>
              <Column offset="1" cols="10">
                <div style={{margin: '50px 0', width: '50%'}}>
                  {isVisibleToast &&
                  <Notification type="warning" icon closeButton handleClose={() => setIsVisibleToast(false)}>
                    <h5 style={{margin: '0 0 5px', color: 'black'}}>Notification title goes here</h5>
                    Provider will capture the full description.
                    <div style={{marginTop: '20px', display: 'flex', justifyContent: 'space-between'}}>
                      <a href="#" title=""><span style={{marginRight: '6px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Action goes here</a>
                    </div>
                  </Notification>
                  }
                  {!isVisibleToast && (
                    <div style={{fontSize: '30px'}}>
                      <a href="#" title="" style={{fontSize: '14px'}} target="_self" onClick={() => setIsVisibleToast(true)}><span style={{marginRight: '5px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Return notification</a>
                      <br />
                      Notification hidden
                    </div>
                  )}
                </div>
              </Column>
            </Row>
          </Container>
        </>
       );
     }}
  </Story>
</Preview>

<Preview>
  <Story name="error notification">
   {() => {
    const [ isVisibleToast, setIsVisibleToast ] = useState(true);
    return (
      <>
        <Container>
          <Row>
            <Column offset="1" cols="10">
              <div style={{margin: '50px 0', width: '100%'}}>
                <Notification type="danger" icon>
                  <h5 style={{margin: '0 0 5px', color: 'black'}}>Notification title goes here</h5>
                  The insurance provider will capture the full description and replacement value of the item(s) you wish to specify later.
                  <div style={{marginTop: '20px', display: 'flex', justifyContent: 'space-between'}}>
                    <a href="#" title=""><span style={{marginRight: '6px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Action goes here</a>
                    <a href="#" title=""><span style={{marginRight: '6px'}}><FontAwesomeIcon icon={faInfoCircle} size="1x" /></span>Action goes here</a>
                  </div>
                </Notification>
              </div>
            </Column>
          </Row>
          <Row>
            <Column offset="1" cols="10">
              <div style={{margin: '50px 0', width: '100%'}}>
                <Notification type="danger" icon>
                  <h5 style={{margin: '0 0 5px', color: 'black'}}>Notification title goes here</h5>
                  The insurance provider will capture the full description and replacement value of the item(s) you wish to specify later.
                  <div style={{marginTop: '20px', display: 'flex', justifyContent: 'space-between'}}>
                    <a href="#" title=""><span style={{marginRight: '6px'}}><FontAwesomeIcon icon={faInfoCircle} size="1x" /></span>What is an Age-Based Discount?</a>
                  </div>
                </Notification>
              </div>
            </Column>
          </Row>
           <Row>
              <Column offset="1" cols="10">
                <div style={{margin: '50px 0', width: '50%'}}>
                  {isVisibleToast &&
                  <Notification type="danger" icon closeButton handleClose={() => setIsVisibleToast(false)}>
                    <h5 style={{margin: '0 0 5px', color: 'black'}}>Notification title goes here</h5>
                    Provider will capture the full description.
                     <div style={{marginTop: '20px', display: 'flex', justifyContent: 'space-between'}}>
                        <a href="#" title=""><span style={{marginRight: '6px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Action goes here</a>
                      </div>
                  </Notification>
                  }
                  {!isVisibleToast && (
                    <div style={{fontSize: '30px'}}>
                      <a href="#" title="" style={{fontSize: '14px'}} target="_self" onClick={() => setIsVisibleToast(true)}><span style={{marginRight: '5px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Return notification</a>
                      <br />
                      Notification hidden
                    </div>
                  )}
                </div>
              </Column>
            </Row>
        </Container>
      </>
    );
   }}
  </Story>
</Preview>


<Props of={Notification} />


