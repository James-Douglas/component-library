import { useState } from 'react';
import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, boolean} from '@storybook/addon-knobs';
import Notification from '../../components/Notification/Notification.component';
import Container from '../../components/Grid/Container/Container.component';
import Row from '../../components/Grid/Row/Row.component';
import Column from '../../components/Grid/Column/Column.component';
import { FontAwesomeIcon } from '@fortawesome/react-fontawesome';
import { faLongArrowLeft } from '@fortawesome/pro-regular-svg-icons/faLongArrowLeft';
import { faInfoCircle } from '@fortawesome/pro-regular-svg-icons/faInfoCircle';

<Meta title="Components|Notifications/Inline" component={Notification} />

# Inline Notifications

### General Notification

<Preview>
  <Story name="inline general notification">
    {() => {
      const [ demo1, setDemo1 ] = useState(true);
      const [ demo2, setDemo2 ] = useState(true);
      const [ demo3, setDemo3 ] = useState(true);
      return (
        <Container>
          <Row>
            <Column offset="1" cols="10">
              {demo1 &&
                <Notification
                  variant="general" 
                  title="Notification title goes here"
                  content="The insurance provider will capture the full description and replacement value of the item(s) you wish to specify later"
                  primaryAction={
                    {
                      content: 'Action goes here' ,
                      link: '#',
                    }
                  }
                  secondaryAction={
                    {
                      content: 'Action goes here' ,
                      link: '#',
                    }
                  }
                  icon 
                  closeButton 
                  handleClose={() => setDemo1(false)}
                >
                </Notification>
              }
              {!demo1 && (
                <div style={{fontSize: '30px'}}>
                  <a href="#" title="" style={{fontSize: '14px'}} target="_self" onClick={() => setDemo1(true)}><span style={{marginRight: '5px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Return Notification</a>
                  <br />
                  Top Notification hidden
                </div>
              )}
            </Column>
          </Row>
          <Row>
            <Column offset="1" cols="10">
              {demo2 &&
                <Notification
                  variant="general" 
                  title="Notification title goes here"
                  content="The insurance provider will capture the full description and replacement value of the item(s) you wish to specify later"
                  primaryAction={
                    {
                      content: 'Action goes here' ,
                      link: '#',
                    }
                  }
                  secondaryAction={
                    {
                      content: 'Action goes here' ,
                      link: '#',
                    }
                  }
                  icon 
                  closeButton 
                  handleClose={() => setDemo2(false)}
                >
                </Notification>
              }
                {!demo2 && (
                  <div style={{fontSize: '30px'}}>
                    <a href="#" title="" style={{fontSize: '14px'}} target="_self" onClick={() => setDemo2(true)}><span style={{marginRight: '5px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Return Notification</a>
                    <br />
                    Middle Notification hidden
                  </div>
                )}
            </Column>
          </Row>
          <Row>
            <Column offset="1" cols="10">
              <div style={{width: '50%'}}>
                {demo3 &&
                  <Notification
                    variant="general" 
                    title="Notification title goes here"
                    content="Provider will capture the full description"
                    primaryAction={
                      {
                        content: 'Action goes here' ,
                        link: '#',
                      }
                    }
                    secondaryAction={
                      {
                        content: 'Action goes here' ,
                        link: '#',
                      }
                    }
                    icon 
                    closeButton 
                    handleClose={() => setDemo3(false)}
                  />
                }
                {!demo3 && (
                  <div style={{fontSize: '30px'}}>
                    <a href="#" title="" style={{fontSize: '14px'}} target="_self" onClick={() => setDemo3(true)}><span style={{marginRight: '5px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Return Notification</a>
                    <br />
                    Bottom Notification hidden
                  </div>
                )}
              </div>
            </Column>
          </Row>
        </Container>
      );
    }}
  </Story>
</Preview>

### Success Notification

<Preview>
  <Story name="inline success notification">
    {() => {
      const [ demo1, setDemo1 ] = useState(true);
      const [ demo2, setDemo2 ] = useState(true);
      const [ demo3, setDemo3 ] = useState(true);
      return (
        <Container>
          <Row>
            <Column offset="1" cols="10">
              {demo1 &&
                <Notification
                  variant="success" 
                  title="Notification title goes here"
                  content="The insurance provider will capture the full description and replacement value of the item(s) you wish to specify later"
                  primaryAction={
                    {
                      content: 'Action goes here' ,
                      link: '#',
                    }
                  }
                  secondaryAction={
                    {
                      content: 'Action goes here' ,
                      link: '#',
                    }
                  }
                  icon 
                  closeButton 
                  handleClose={() => setDemo1(false)}
                >
                </Notification>
              }
              {!demo1 && (
                <div style={{fontSize: '30px'}}>
                  <a href="#" title="" style={{fontSize: '14px'}} target="_self" onClick={() => setDemo1(true)}><span style={{marginRight: '5px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Return Notification</a>
                  <br />
                  Top Notification hidden
                </div>
              )}
            </Column>
          </Row>
          <Row>
            <Column offset="1" cols="10">
              {demo2 &&
                <Notification
                  variant="success" 
                  title="Notification title goes here"
                  content="The insurance provider will capture the full description and replacement value of the item(s) you wish to specify later"
                  primaryAction={
                    {
                      content: 'Action goes here' ,
                      link: '#',
                    }
                  }
                  secondaryAction={
                    {
                      content: 'Action goes here' ,
                      link: '#',
                    }
                  }
                  icon 
                  closeButton 
                  handleClose={() => setDemo2(false)}
                >
                </Notification>
              }
                {!demo2 && (
                  <div style={{fontSize: '30px'}}>
                    <a href="#" title="" style={{fontSize: '14px'}} target="_self" onClick={() => setDemo2(true)}><span style={{marginRight: '5px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Return Notification</a>
                    <br />
                    Middle Notification hidden
                  </div>
                )}
            </Column>
          </Row>
          <Row>
            <Column offset="1" cols="10">
              <div style={{width: '50%'}}>
                {demo3 &&
                  <Notification 
                    variant="success" 
                    title="Notification title goes here"
                    content="Provider will capture the full description"
                    primaryAction={
                      {
                        content: 'Action goes here' ,
                        link: '#',
                      }
                    }
                    secondaryAction={
                      {
                        content: 'Action goes here' ,
                        link: '#',
                      }
                    }
                    icon 
                    closeButton 
                    handleClose={() => setDemo3(false)}
                  />
                }
                {!demo3 && (
                  <div style={{fontSize: '30px'}}>
                    <a href="#" title="" style={{fontSize: '14px'}} target="_self" onClick={() => setDemo3(true)}><span style={{marginRight: '5px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Return Notification</a>
                    <br />
                    Bottom Notification hidden
                  </div>
                )}
              </div>
            </Column>
          </Row>
        </Container>
      );
    }}
  </Story>
</Preview>

### Warning Notification

<Preview>
  <Story name="inline warning notification">
    {() => {
      const [ demo1, setDemo1 ] = useState(true);
      const [ demo2, setDemo2 ] = useState(true);
      const [ demo3, setDemo3 ] = useState(true);
      return (
        <Container>
          <Row>
            <Column offset="1" cols="10">
              {demo1 &&
                <Notification
                  variant="warning" 
                  title="Notification title goes here"
                  content="The insurance provider will capture the full description and replacement value of the item(s) you wish to specify later"
                  primaryAction={
                    {
                      content: 'Action goes here' ,
                      link: '#',
                    }
                  }
                  secondaryAction={
                    {
                      content: 'Action goes here' ,
                      link: '#',
                    }
                  }
                  icon 
                  closeButton 
                  handleClose={() => setDemo1(false)}
                >
                </Notification>
              }
              {!demo1 && (
                <div style={{fontSize: '30px'}}>
                  <a href="#" title="" style={{fontSize: '14px'}} target="_self" onClick={() => setDemo1(true)}><span style={{marginRight: '5px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Return Notification</a>
                  <br />
                  Top Notification hidden
                </div>
              )}
            </Column>
          </Row>
          <Row>
            <Column offset="1" cols="10">
              {demo2 &&
                <Notification
                  variant="warning" 
                  title="Notification title goes here"
                  content="The insurance provider will capture the full description and replacement value of the item(s) you wish to specify later"
                  primaryAction={
                    {
                      content: 'Action goes here' ,
                      link: '#',
                    }
                  }
                  secondaryAction={
                    {
                      content: 'Action goes here' ,
                      link: '#',
                    }
                  }
                  icon 
                  closeButton 
                  handleClose={() => setDemo2(false)}
                >
                </Notification>
              }
                {!demo2 && (
                  <div style={{fontSize: '30px'}}>
                    <a href="#" title="" style={{fontSize: '14px'}} target="_self" onClick={() => setDemo2(true)}><span style={{marginRight: '5px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Return Notification</a>
                    <br />
                    Middle Notification hidden
                  </div>
                )}
            </Column>
          </Row>
          <Row>
            <Column offset="1" cols="10">
              <div style={{width: '50%'}}>
                {demo3 &&
                  <Notification 
                    variant="warning" 
                    title="Notification title goes here"
                    content="Provider will capture the full description"
                    primaryAction={
                      {
                        content: 'Action goes here' ,
                        link: '#',
                      }
                    }
                    secondaryAction={
                      {
                        content: 'Action goes here' ,
                        link: '#',
                      }
                    }
                    icon 
                    closeButton 
                    handleClose={() => setDemo3(false)}
                  />
                }
                {!demo3 && (
                  <div style={{fontSize: '30px'}}>
                    <a href="#" title="" style={{fontSize: '14px'}} target="_self" onClick={() => setDemo3(true)}><span style={{marginRight: '5px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Return Notification</a>
                    <br />
                    Bottom Notification hidden
                  </div>
                )}
              </div>
            </Column>
          </Row>
        </Container>
      );
    }}
  </Story>
</Preview>

<Preview>
  <Story name="inline error notification">
    {() => {
      const [ demo1, setDemo1 ] = useState(true);
      const [ demo2, setDemo2 ] = useState(true);
      const [ demo3, setDemo3 ] = useState(true);
      return (
        <Container>
          <Row>
            <Column offset="1" cols="10">
              {demo1 &&
                <Notification
                  variant="error" 
                  title="Notification title goes here"
                  content="The insurance provider will capture the full description and replacement value of the item(s) you wish to specify later"
                  primaryAction={
                    {
                      content: 'Action goes here' ,
                      link: '#',
                    }
                  }
                  secondaryAction={
                    {
                      content: 'Action goes here' ,
                      link: '#',
                    }
                  }
                  icon 
                  closeButton 
                  handleClose={() => setDemo1(false)}
                >
                </Notification>
              }
              {!demo1 && (
                <div style={{fontSize: '30px'}}>
                  <a href="#" title="" style={{fontSize: '14px'}} target="_self" onClick={() => setDemo1(true)}><span style={{marginRight: '5px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Return Notification</a>
                  <br />
                  Top Notification hidden
                </div>
              )}
            </Column>
          </Row>
          <Row>
            <Column offset="1" cols="10">
              {demo2 &&
                <Notification
                  variant="error" 
                  title="Notification title goes here"
                  content="The insurance provider will capture the full description and replacement value of the item(s) you wish to specify later"
                  primaryAction={
                    {
                      content: 'Action goes here' ,
                      link: '#',
                    }
                  }
                  secondaryAction={
                    {
                      content: 'Action goes here' ,
                      link: '#',
                    }
                  }
                  icon 
                  closeButton 
                  handleClose={() => setDemo2(false)}
                >
                </Notification>
              }
                {!demo2 && (
                  <div style={{fontSize: '30px'}}>
                    <a href="#" title="" style={{fontSize: '14px'}} target="_self" onClick={() => setDemo2(true)}><span style={{marginRight: '5px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Return Notification</a>
                    <br />
                    Middle Notification hidden
                  </div>
                )}
            </Column>
          </Row>
          <Row>
            <Column offset="1" cols="10">
              <div style={{width: '50%'}}>
                {demo3 &&
                  <Notification 
                    variant="error" 
                    title="Notification title goes here"
                    content="Provider will capture the full description"
                    primaryAction={
                      {
                        content: 'Action goes here' ,
                        link: '#',
                      }
                    }
                    secondaryAction={
                      {
                        content: 'Action goes here' ,
                        link: '#',
                      }
                    }
                    icon 
                    closeButton 
                    handleClose={() => setDemo3(false)}
                  />
                }
                {!demo3 && (
                  <div style={{fontSize: '30px'}}>
                    <a href="#" title="" style={{fontSize: '14px'}} target="_self" onClick={() => setDemo3(true)}><span style={{marginRight: '5px'}}><FontAwesomeIcon icon={faLongArrowLeft} size="1x" /></span>Return Notification</a>
                    <br />
                    Bottom Notification hidden
                  </div>
                )}
              </div>
            </Column>
          </Row>
        </Container>
      );
    }}
  </Story>
</Preview>


<Props of={Notification} />


