import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import FieldValidation from '../../components/FieldValidation/FieldValidation.component';

<Meta title="Components|FieldValidation" component={FieldValidation} />

# FieldValidation

Renders styled validation error message. This component is included in the Fieldset component (which wraps form components),
so there shouldn't be a need to use it directly.

### Props

<Props of={FieldValidation} />
