import { useState } from 'react';
import DateRangePicker from '../../components/DatePicker/DateRangePicker.component';
import moment from 'moment';
import FluidContainer from '../../components/Grid/Container/FluidContainer.component';
import Row from '../../components/Grid/Row/Row.component';
import Column from '../../components/Grid/Column/Column.component';

<Meta title="Components|DatePicker|DateRangePicker" component={DateRangePicker} />

#SingleDatePicker

<Preview>
  <Story name="range datepicker">
    {() => {
    const [startDate, setStartDate] = useState(moment('2020-03-20T00:00:00.000'));
    const [endDate, setEndDate] = useState(moment('2020-04-20T00:00:00.000'));
      return (
        <div style={{marginTop: '30px'}}>
          <FluidContainer>
            <Row>
                <DateRangePicker
                  startDateId="start-date"
                  startDateTooltip={{ title: 'Start Date' }}
                  startDateAriaLabel="Start Date"
                  startDateValue={startDate}
                  endDateId="end-date"
                  endDateTooltip={{ title: 'End Date' }}
                  endDateAriaLabel="End Date"
                  endDateValue={endDate}
                  numberOfMonths={2}
                  endDateValidationMessage="Incorrect end date"
                  startDateValidationMessage="Incorrect start date"
                  isDayBlocked={(day) => day.weekday() === 4}
                />
            </Row>
          </FluidContainer>
        </div>
      );
    }}
  </Story>
</Preview>
