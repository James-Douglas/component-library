import { Meta, Story, Preview, Props } from '@storybook/addon-docs/blocks';
import { withKnobs, boolean} from '@storybook/addon-knobs';
import Callout from '../../components/Callout/Callout.component';
import FluidContainer from '../../components/Grid/Container/Fluid.component';
import Row from '../../components/Grid/Row/Row.component';
import Column from '../../components/Grid/Column/Column.component';


<Meta title="Components|Callout" component={Callout} />

# Callout

<Preview>
  <Story name="callout">
    <FluidContainer>
      <Row>
        <Column>
          <Callout>The insurance provider will capture the full description and replacement value of the item(s) you wish to specify later.</Callout>
        </Column>
      </Row>
    </FluidContainer>
  </Story>
</Preview>

<Props of={Callout} />

<Story name="callout with props" decorators={[withKnobs]} parameters={{ docs: { disable: true } }}>
    {() => {
      const bg = boolean('bgColorGrey', true);
      return (
        <FluidContainer>
          <Row>
            <Column>
              <Callout bgColorGrey={bg}>The insurance provider will capture the full description and replacement value of the item(s) you wish to specify later.</Callout>
            </Column>
          </Row>
        </FluidContainer>
      );
    }}
  </Story>

